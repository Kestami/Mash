export default class TutorialItem {
    text: string;
    gifLocation: string;

    constructor(text: string, gifLocation: string) {
        this.text = text;
        this.gifLocation = gifLocation;
    }
}